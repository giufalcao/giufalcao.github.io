---
import SectionHeader from '../ui/SectionHeader.astro';
import SkillPill from '../ui/SkillPill.astro';
import { skillGroups } from '../../data/skills';
---

<section class="section-padding bg-surface-1/30 border-y border-border">
  <div class="max-w-6xl mx-auto">
    <SectionHeader
      id="skills"
      title="Skills & Tools"
      subtitle="Ordered by depth — not alphabetically."
    />

    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
      {skillGroups.map((group) => (
        <div class="card">
          <h3 class="text-sm font-semibold text-text-secondary uppercase tracking-wider font-mono mb-4">
            {group.label}
          </h3>
          <div class="flex flex-wrap gap-2">
            {group.skills.map((skill) => (
              <SkillPill name={skill.name} tier={skill.tier} colorScheme={group.colorScheme} />
            ))}
          </div>
        </div>
      ))}
    </div>

    <p class="text-xs text-text-muted font-mono mt-6 text-center">
      <span class="text-accent">●</span> = core proficiency
    </p>
  </div>
</section>
